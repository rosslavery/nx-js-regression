# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build broken-node`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t broken-node`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system broken-node && \
          adduser --system -G broken-node broken-node

COPY dist/apps/broken-node broken-node
RUN chown -R broken-node:broken-node .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix broken-node --omit=dev -f install

CMD [ "node", "broken-node" ]
